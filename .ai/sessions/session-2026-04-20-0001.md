# Session 2026-04-20-0001: Admin REST API 복원

## 수행 내용
- admin/api.py: 6개 REST 엔드포인트 (complexes CRUD, regulations/meetings/documents 조회, ingest)
- admin/auth.py: AdminApiKeyMiddleware (X-Admin-API-Key 헤더 + ADMIN_API_KEY env)
- ingest/service.py: HTTP용 인제스트 서비스 래퍼 (CLI 로직 재사용)
- server.py: Mount("/admin/api", ...) 추가, FastMCP 루트 Mount 앞에 배치
- .env.example 생성, AGENTS.md 업데이트 (책임 경계 + API 표 + 환경변수)

## 스모크 결과
- 401 (no key) / 401 (wrong key) / 200 (correct key) 모두 정상
- healthz: 200 ok
- /mcp: FastMCP 정상 응답 (회귀 없음)

## 다음 세션 ToDo
- apt-web 측 asyncpg 직접 접근 -> httpx로 /admin/api/* 경유하도록 변환 (별도 지시서)
- apt-legal-agent/docs/01_architecture.md 업데이트 (.ai/portal-instructions-admin-rest.md 참조)
- 포털 배포 후 ADMIN_API_KEY secret 주입
